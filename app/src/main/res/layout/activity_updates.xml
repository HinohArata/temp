<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground">

    <!-- Container Fragment (Isi Halaman) -->
    <!-- layout_above tidak wajib jika Anda ingin konten discroll di belakang navbar,
         tapi pastikan list di fragment punya paddingBottom -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- WRAPPER: Material Card View untuk efek Floating -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/nav_card_wrapper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="16dp"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        app:cardCornerRadius="24dp"
        app:cardElevation="12dp"
        app:cardBackgroundColor="?android:attr/colorBackgroundFloating"
        app:strokeWidth="0dp">

        <!-- NAVIGATION VIEW -->
        <!-- Background diset transparan agar mengikuti bentuk rounded CardView -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:elevation="0dp"
            app:itemIconTint="@color/nav_item_color_state"
            app:itemTextColor="@color/nav_item_color_state"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/menu_bottom_nav" />

    </com.google.android.material.card.MaterialCardView>

</RelativeLayout>
